name: Build Flutter Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Setup Java 17 (required for Gradle & Android SDK)
      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # 3. Setup Flutter (Stable)
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.7'
          channel: 'stable'
          cache: true

      # 4. Verify Flutter version
      - name: Verify Flutter version
        run: flutter --version

      # 5. Accept Android licenses (now Java + SDK are ready)
      - name: Accept Android Licenses
        run: yes | flutter doctor --android-licenses

      # 6. Add swap space (prevent OOM)
      - name: Add swap space
        run: |
          sudo fallocate -l 4G /swapfile
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile

      # 7. Clean project and get dependencies
      - name: Full clean before build
        run: |
          rm -rf ~/.gradle/caches
          flutter clean
          flutter pub get

      # 8. Build split APKs (verbose, no shrink)
      - name: Build split APKs
        run: flutter build apk --release --split-per-abi --no-shrink -v

      # 9. Build App Bundle (AAB, verbose, no shrink)
      - name: Build App Bundle
        run: flutter build appbundle --release --no-shrink -v

      # 10. Upload release artifacts
      - name: Upload release artifacts
        uses: actions/upload-artifact@v4
        with:
          name: anymex-tv-multiarch-release
          path: |
            build/app/outputs/flutter-apk/app-arm64-v8a-release.apk
            build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk
            build/app/outputs/flutter-apk/app-x86_64-release.apk
            build/app/outputs/bundle/release/app-release.aab
